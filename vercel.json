{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "node scripts/build.js",
        "outputDirectory": "dist"
      }
    }
  ]
}
